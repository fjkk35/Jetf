@{
    ViewBag.Title = "DownloadSeaNew";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div ng-controller="DownloadSeaNewController">
    <span class="headtype">物流代收檔下載-海運(新)</span>
    <hr />

    <form id="DownloadSeaNewForm"
          action="@Url.Action("SeaExcel", "DownloadSeaNew")"
          method="post"
          enctype="multipart/form-data"
          class="containertype">
        <div class="col-lg-12" style="margin-bottom:0.5rem;">
            <label for="date">日　　期</label>
            <input type="text"
                   id="date"
                   name="date"
                   class="datepicker-input form-control"
                   ng-model="form.date"
                   uib-datepicker-popup="yyyy-MM-dd"
                   datepicker-options="dateOptions"
                   is-open="datePopup.opened"
                   ng-click="openDatePopup()"
                   close-text="關閉"
                   current-text="今天"
                   show-button-bar="false"
                   readonly
                   placeholder="請選擇日期" />
        </div>
        <div class="col-lg-12" style="margin-bottom:0.5rem;">
            <label for="taxType">稅金種類</label>
            <select id="taxType"
                    name="taxType"
                    class="form-control"
                    ng-model="form.taxType">
                <option value="">請選擇</option>
                <option ng-repeat="item in taxTypeList" value="{{item.Value}}">{{item.Text}}</option>
            </select>
        </div>

        <button type="button"
                class="btn btn-primary confirmbtn col-lg-3"
                style="font-size: 1.2rem;"
                ng-click="downloadAll()"
                ng-disabled="loading">
            <i class="fas fa-download"></i> 下載
        </button>

        <div class="loader loader-bar" ng-class="{'is-active': loading}" data-rounded data-text="檔案下載中"></div>
    </form>
</div>
