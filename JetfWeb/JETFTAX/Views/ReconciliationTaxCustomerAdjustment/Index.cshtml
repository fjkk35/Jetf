@{
    ViewBag.Title = "稅金客戶調整";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<style>
    .tax-customer-adjustment-page {
        padding: 0 18px 18px;
        box-sizing: border-box;
    }

    .tax-customer-adjustment-summary {
        display: flex;
        flex-wrap: wrap;
        gap: 12px;
        margin: 18px 0;
    }

    .tax-customer-adjustment-summary-card {
        min-width: 180px;
        padding: 14px 18px;
        border-radius: 8px;
        background: #f2f7ff;
        border: 1px solid #d7dee6;
        border-left: 5px solid #2f80ed;
    }

    .tax-customer-adjustment-summary-card.summary-success {
        border-left-color: #2fb344;
        background: #f0fbf3;
    }

    .tax-customer-adjustment-summary-card.summary-fail {
        border-left-color: #e83e8c;
        background: #fff3f8;
    }

    .tax-customer-adjustment-summary-card .label {
        display: block;
        color: #6c757d;
        margin-bottom: 8px;
    }

    .tax-customer-adjustment-summary-card .value {
        font-size: 24px;
        font-weight: 600;
        color: #1f2d3d;
    }

    .tax-customer-adjustment-table th {
        white-space: nowrap;
    }

    .tax-customer-adjustment-table .status-fail {
        color: #e53935;
        font-weight: 600;
    }
</style>

<div class="tax-customer-adjustment-page"
     ng-controller="ReconciliationTaxCustomerAdjustmentController">
    <span class="headtype">稅金客戶調整</span>
    <hr />

    <div class="containertype">
        <div class="form-group row">
            <label class="col-lg-1 col-md-3 col-sm-12 control-label"
                   style="padding-top:7px;">
                選擇檔案
            </label>
            <div class="col-lg-6 col-md-7 col-sm-12"
                 style="margin-bottom:0.5rem;">
                <input type="file"
                       id="taxCustomerAdjustmentFileInput"
                       accept=".xlsx"
                       class="form-control" />
            </div>
        </div>

        <div class="form-group row" style="margin-bottom:0;">
            <div class="col-lg-offset-1 col-md-offset-3 col-lg-11 col-md-9 col-sm-12"
                 style="margin-top:0.5rem;">
                <a href="@Url.Action("DownloadTemplate", "ReconciliationTaxCustomerAdjustment")"
                   target="_blank">
                    下載範例
                </a>
            </div>
        </div>

        <div class="form-group row" style="margin-bottom:0;">
            <div class="col-lg-offset-1 col-md-offset-3 col-lg-11 col-md-9 col-sm-12"
                 style="margin-top:0.5rem;">
                <button type="button"
                        class="btn btn-primary confirmbtn"
                        ng-click="uploadFile()"
                        ng-disabled="uploading">
                    <i class="fas fa-upload"></i>
                    {{ uploading ? '上傳中...' : '上傳' }}
                </button>
            </div>
        </div>

        <div class="loader loader-bar"
             ng-class="{'is-active': uploading}"
             data-rounded
             data-text="檔案上傳中"></div>

        <div ng-if="uploadSummary">
            <div class="tax-customer-adjustment-summary">
                <div class="tax-customer-adjustment-summary-card">
                    <span class="label">上傳筆數</span>
                    <span class="value">{{ uploadSummary.Count }}</span>
                </div>
                <div class="tax-customer-adjustment-summary-card summary-success">
                    <span class="label">成功筆數</span>
                    <span class="value">{{ uploadSummary.UpdatedCount }}</span>
                </div>
                <div class="tax-customer-adjustment-summary-card summary-fail">
                    <span class="label">失敗筆數</span>
                    <span class="value">{{ uploadSummary.FailCount }}</span>
                </div>
            </div>

            <h4>處理結果（共 {{ uploadData.length }} 筆）</h4>
            <div class="table-responsive">
                <table class="table table-bordered table-hover tax-customer-adjustment-table">
                    <thead>
                        <tr>
                            <th>Excel 列號</th>
                            <th>分提單號</th>
                            <th>物流貨號</th>
                            <th>新客戶代號</th>
                            <th>狀態</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr ng-repeat="item in uploadData track by $index">
                            <td>{{ item.RowNo }}</td>
                            <td>{{ item.TrackingNo }}</td>
                            <td>{{ item.DlvInv }}</td>
                            <td>{{ item.NewCustomerCode }}</td>
                            <td ng-class="{'status-fail': !item.IsSuccess}">
                                {{ item.Status }}
                            </td>
                        </tr>
                        <tr ng-if="uploadData.length === 0">
                            <td colspan="5" class="text-center">查無資料</td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>
